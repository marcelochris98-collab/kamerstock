{
  "$schema": "https://schema.railpack.com",
  "provider": "php",
  "packages": {
    "php": "8.3"
  },
  "variables": {
    "COMPOSER_ALLOW_SUPERUSER": "1"
  },
  "steps": {
    "install": {
      "commands": [
        "composer install --no-dev --optimize-autoloader --no-interaction --ignore-platform-req=ext-*",
        "npm ci"
      ]
    },
    "build": {
      "commands": [
        "npm run build",
        "mkdir -p storage/framework/sessions storage/framework/views storage/framework/cache storage/framework/testing storage/logs bootstrap/cache",
        "chmod -R a+rw storage bootstrap/cache",
        "php artisan config:cache",
        "php artisan route:cache",
        "php artisan view:cache"
      ]
    }
  },
  "deploy": {
    "startCommand": "php artisan migrate --force && php artisan storage:link || true && frankenphp php-server -r public --listen 0.0.0.0:$PORT"
  }
}